<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Flashcards" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="actionOverflowButtonStyle">@style/MyOverflowButtonStyle</item>
    </style>


    <!-- Style for the overflow button -->
    <style name="MyOverflowButtonStyle" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <!-- Set your custom icon here -->
        <item name="android:src">@drawable/ic_action_name</item>
        <item name="android:tint">@android:color/black</item>
    </style>
</resources>